---
import "../styles/global.css";

export interface Props {
  title?: string;
  description?: string;
  canonical?: string;
}

const {
  title = "Numericano — unit conversions with clear assumptions",
  description = "Convert cooking volumes to weights using typical ingredient densities. Fast, explicit, and consistent.",
  canonical,
} = Astro.props;

const siteUrl = "https://numericano.com";
const canonicalUrl = canonical ?? (siteUrl + Astro.url.pathname);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <title>{title}</title>
  </head>

  <body class="min-h-screen bg-slate-50 text-slate-900 antialiased">
    <a
      href="#content"
      class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-lg focus:bg-white focus:px-4 focus:py-2 focus:text-sm focus:shadow"
    >
      Skip to content
    </a>

    <header class="sticky top-0 z-40 border-b border-slate-200 bg-white/80 backdrop-blur">
      <div class="mx-auto flex max-w-6xl items-center justify-between gap-4 px-4 py-3">
        <a href="/" class="flex items-center gap-2 font-semibold tracking-tight">
          <span class="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-slate-900 text-white text-sm">N</span>
          <span>Numericano</span>
        </a>

        <nav class="flex items-center gap-4 text-sm text-slate-700">
          <a class="rounded-md px-2 py-1 hover:bg-slate-100 hover:text-slate-900" href="/cooking/">Cooking</a>
          <a class="rounded-md px-2 py-1 hover:bg-slate-100 hover:text-slate-900" href="/volume-weight/">Volume ↔ Weight</a>
          <a class="rounded-md px-2 py-1 hover:bg-slate-100 hover:text-slate-900" href="/ingredients/sugar/">Ingredients</a>
        </nav>
      </div>
    </header>

    <main id="content" class="mx-auto max-w-6xl px-4 py-8">
      <div class="grid gap-6 lg:grid-cols-[1fr_320px]">
        <article
          class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm
                 prose prose-slate max-w-none
                 prose-a:text-blue-700 prose-a:font-medium prose-a:no-underline hover:prose-a:underline
                 prose-headings:tracking-tight
                 prose-h1:text-3xl prose-h2:text-2xl"
        >
          <slot />
        </article>

        <aside class="hidden lg:block">
          <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
            <div class="text-sm font-semibold text-slate-900">Quick paths</div>
            <ul class="mt-3 space-y-2 text-sm">
              <li><a class="text-blue-700 hover:underline" href="/ingredients/water/">Water (baseline)</a></li>
              <li><a class="text-blue-700 hover:underline" href="/ingredients/sugar/">Sugar</a></li>
              <li><a class="text-blue-700 hover:underline" href="/ingredients/flour/">Flour</a></li>
              <li><a class="text-blue-700 hover:underline" href="/cooking/">Cooking hub</a></li>
              <li><a class="text-blue-700 hover:underline" href="/volume-weight/">Volume ↔ Weight hub</a></li>
            </ul>

            <div class="mt-5 border-t pt-4 text-xs text-slate-500">
              Numericano prioritizes clarity: each conversion is tied to an explicit assumption (density, packing, temperature).
            </div>
          </div>
        </aside>
      </div>
    </main>

    <footer class="border-t border-slate-200 bg-white">
      <div class="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-6 text-sm text-slate-600 sm:flex-row sm:items-center sm:justify-between">
        <div>© {new Date().getFullYear()} Numericano</div>
        <div class="flex gap-4">
          <a class="hover:underline" href="/about/">About</a>
        </div>
      </div>
    </footer>
  </body>
</html>
