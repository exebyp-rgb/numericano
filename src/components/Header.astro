---
const nav = [
  { label: "Conversions", href: "/conversions/" },
  { label: "Volume ↔ Weight", href: "/volume-weight/" },
  { label: "Ingredients", href: "/ingredients/" }
];
---

<header class="border-b border-slate-200">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
    <a href="/" class="text-lg font-semibold text-slate-900 hover:text-blue-600">
      Numericano
    </a>

    <nav class="hidden items-center gap-6 md:flex">
      {nav.map((i) => (
        <a
          href={i.href}
          class="text-sm text-slate-600 hover:text-blue-600"
        >
          {i.label}
        </a>
      ))}
      <a class="rounded border border-blue-600 px-3 py-1.5 text-sm text-blue-600 hover:bg-blue-50" href="/ingredients/">
        Browse
      </a>
    </nav>

    <div class="flex items-center gap-2 md:hidden">
      <a class="rounded border border-blue-600 px-3 py-1.5 text-sm text-blue-600 hover:bg-blue-50" href="/ingredients/">
        Menu
      </a>
    </div>
  </div>
</header>
