---
const quick = [
  { label: "Water (baseline)", href: "/ingredients/water/" },
  { label: "Sugar", href: "/ingredients/sugar/" },
  { label: "Flour", href: "/ingredients/flour/" },
  { label: "Cooking hub", href: "/conversions/" },
  { label: "Volume ↔ Weight hub", href: "/volume-weight/" }
];

const tips = [
  "Every conversion ties to an explicit assumption (density, packing, temperature).",
    'If your measurement is "heaping" or "sifted", choose the closest variant.',
  "When in doubt: weigh it once, then reuse your own baseline."
];
---
<aside class="space-y-4">
  <div class="panel overflow-hidden">
    <div class="h-1.5 rainbow-line"></div>
    <div class="p-4">
      <p class="text-xs font-semibold uppercase tracking-wider text-white/60">Quick paths</p>
      <ul class="mt-3 space-y-1">
        {quick.map((q) => (
          <li>
            <a
              href={q.href}
              class="block rounded-xl px-3 py-2 text-sm text-white/75 hover:bg-white/5 hover:text-white"
            >
              {q.label}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>

  <div class="panel-soft p-4">
    <p class="text-xs font-semibold uppercase tracking-wider text-white/60">Assumptions</p>
    <ul class="mt-3 space-y-2 text-sm text-white/70">
      {tips.map((t) => (
        <li class="flex gap-2">
          <span class="mt-1 h-1.5 w-1.5 flex-none rounded-full bg-white/40"></span>
          <span>{t}</span>
        </li>
      ))}
    </ul>
  </div>
</aside>
